
import {AppRegistry} from 'react-native';
import App from './App';
import {name as appName} from './app.json';

// import Cpntwo from './components/cpntwo';

AppRegistry.registerComponent(appName, () => App);  // chọn màn hình chạy đầu tiên

// AppRegistry.registerComponent(appName, () => Cpntwo);


